{
  "slug": "excel-reader-toolkit",
  "display_name": "Excel Reader Toolkit",
  "description": "Reads an Excel file and exports its contents into a text file",
  "version": "0.1.0",
  "entrypoint": {
    "type": "python",
    "command": "python main.py"
  },
  "capabilities": [
    {
      "name": "read_excel_to_text",
      "description": "Reads an Excel file and returns/writes its content",
      "schema": {
        "type": "object",
        "properties": {
          "file_path": {
            "type": "string",
            "description": "Full path to the Excel file"
          }
        },
        "required": ["file_path"]
      }
    }
  ],
  "author": "Neehar",
  "tags": ["excel", "file-processing", "text-export"]
}
